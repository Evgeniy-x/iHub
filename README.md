Test project for RestoWaves

To run the project, download the repository locally. Execute the command:

- npm instal
- npm start

Open [http://localhost:3000](http://localhost:3000) to view it in your browser.
